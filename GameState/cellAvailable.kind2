use Bool/{false, true}
use GameState/{inProgress, draw, winner}
use Maybe/{some, none}

cellAvailable
- state: GameState
: Bool

match state {
  inProgress:
    let occupiedCells = (List/map Move (Cell _) state.board Move/getCell)
    let cellOccupied = (List/find _ λcell (Cell/eq cell state.cursorPosition) occupiedCells)
    match cellOccupied {
      some: false
      none: true
    }
  draw: false
  winner: false
}
